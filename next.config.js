/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'cdn.astria.ai',
      'replicate.delivery',
      'pbxt.replicate.delivery',
    ],
  },
}

module.exports = nextConfig
